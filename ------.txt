移除 GitHub 時期的 JSON 編輯後台 — 請在 repo 裡刪掉這些檔案／資料夾
（更新包裡只放「有修改」的檔案，刪除的部分要在 GitHub 上手動刪）

整個資料夾刪掉：
  admin/                    ← Sveltia CMS 後台介面
    admin/index.html
    admin/config.yml

單獨刪掉：
  content/site.json         ← 公司資訊（電話／信箱／版權）
  content/pages.json        ← 頁面文字改寫對應表（26KB）
  src/content.js            ← 後台內容套用層

已修改（用更新包覆蓋）：
  contact.html              ← 移除頁尾兩支 script 載入、修正一處註解
  github.md                 ← 更新同步紀錄


保留，不要刪：
  content/timeline.js       ← 不是後台，是時間軸的離線備援快照。
                               about.html 仍在載入它（目前 render 不讀它，
                               所以留著也只是多一支請求）。要一起清掉的話
                               跟我說，我把 about.html 那行一併移除。

  頁面上的 data-site / data-site-href 標記
                            ← contact.html 的通知信箱是從 [data-site="email"]
                               元素讀的，不是從 JSON，刪掉會壞。
                               日後 D1 編輯頁也可以沿用這些標記。

  apps-script/Code.gs       ← 只有註解提到 site.json，功能不受影響。
  tools/i18n-audit.html     ← 同上，只有一行註解提到「後台」。
