完全改用 D1 —— 請在 repo 裡刪掉這些檔案
（更新包只放「有修改」的檔案，刪除的部分要在 GitHub 上手動刪）

■ 資料快照（已全部搬進 D1）
  content/timeline.js            ← window.TIMELINE 時間軸快照
  content/site.json              ← 公司資訊（前一批就要刪的）
  content/pages.json             ← 頁面文字改寫表（前一批就要刪的）
  src/data/products-data.js      ← window.PRODUCT_DATA 產品快照
  src/data/products-data-en.csv  ← 英文欄位的 CSV 副本
  src/data/embeddings.json       ← 舊語意搜尋的向量檔
  → content/ 與 src/data/ 兩個資料夾刪完就空了，可一併刪除

■ 只為了「產生那些快照」而存在的工具
  tools/products-excel.html      ← Excel → products-data.js 轉檔
  tools/timeline-excel.html      ← Excel → timeline.js 轉檔
  tools/build-embeddings.html    ← 產生 embeddings.json
  src/search-records.js          ← 只被 build-embeddings 呼叫

■ 舊的語意搜尋引擎（依賴 embeddings.json，任何頁面都沒有載入）
  src/site-search-vector-backup.js

■ JSON 編輯後台（前一批就要刪的）
  admin/                         ← 整個資料夾
  src/content.js


已修改（用更新包覆蓋）
  src/site-search.js       ← 【重要】原本會動態注入 products-data.js 當資料來源，
                              已改為 fetch /api/products。不換這支的話，
                              刪掉快照後全站搜尋會靜默失效（0 筆但不報錯）。
  about.html               ← 移除 content/timeline.js 載入
  tools/i18n-audit.html    ← 改讀 /api/products（這支是有用的稽核工具，故保留）
  contact.html             ← 移除後台套用層載入
  github.md                ← 更新同步紀錄


部署後請確認這三件事
  1. 全站搜尋：任一頁按 Ctrl+K，打「垃圾袋」應出現清潔袋
  2. 關於營德：時間軸有資料且會淡入
  3. 首頁與產品中心：七張產品卡片都在

注意：搜尋現在需要 API 才有資料，用 file:// 直接開會搜不到東西
      （本機測試請用 npx wrangler pages dev .）
